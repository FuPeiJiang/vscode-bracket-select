select switch case<br>
select if blocks

to build and install:<br>
`yarn w`

to use debugger:<br>
`yarn build` (only first the first time when you clone this repo)<br>
then "Run Extension" (this should call `yarn watch`)
